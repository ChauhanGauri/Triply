<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || "Admin Login" %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      display: flex;
      background: #f5f5f7;
    }
    
    .login-wrapper {
      display: flex;
      width: 100%;
      min-height: 100vh;
    }

    /* Left Panel - Image */
    .left-panel {
      flex: 1;
      background: linear-gradient(135deg, #F59E0B 0%, #D97706 100%);
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .left-panel::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('https://images.unsplash.com/photo-1544620347-c4fd4a3d5957?w=800') center/cover;
      opacity: 0.25;
    }

    .left-content {
      position: relative;
      z-index: 2;
      text-align: center;
      color: white;
      padding: 3rem;
      max-width: 500px;
    }

    .left-content .logo {
      font-size: 3rem;
      margin-bottom: 1.5rem;
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-20px); }
    }

    .left-content h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      line-height: 1.2;
    }

    .left-content p {
      font-size: 1.1rem;
      opacity: 0.95;
      line-height: 1.6;
    }

    .carousel-indicators {
      position: absolute;
      bottom: 30px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      gap: 8px;
    }

    .carousel-indicators button {
      width: 30px;
      height: 4px;
      background: rgba(255, 255, 255, 0.5);
      border: none;
      border-radius: 2px;
      cursor: pointer;
      transition: all 0.3s;
    }

    .carousel-indicators button.active {
      background: white;
      width: 40px;
    }

    /* Right Panel - Form */
    .right-panel {
      flex: 1;
      background: #2d3748;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    
    .login-container {
      width: 100%;
      max-width: 450px;
      animation: fadeInRight 0.6s ease-out;
    }

    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: #a0aec0;
      text-decoration: none;
      font-size: 0.9rem;
      margin-bottom: 2rem;
      transition: color 0.3s;
    }

    .back-link:hover {
      color: #F59E0B;
    }

    .login-header {
      margin-bottom: 2rem;
    }
    
    .login-header h2 {
      color: white;
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }
    
    .login-header p {
      color: #a0aec0;
      font-size: 1rem;
    }

    .admin-badge {
      display: inline-block;
      background: linear-gradient(135deg, #F59E0B 0%, #D97706 100%);
      color: white;
      padding: 0.4rem 1rem;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }
    
    .form-label {
      color: #cbd5e0;
      font-size: 0.9rem;
      margin-bottom: 0.5rem;
      font-weight: 500;
      display: block;
    }

    .form-control {
      border-radius: 8px;
      border: 1px solid #4a5568;
      padding: 12px 16px;
      font-size: 1rem;
      transition: all 0.3s ease;
      background: #1a202c;
      color: white;
      width: 100%;
    }
    
    .form-control:focus {
      border-color: #F59E0B;
      box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.1);
      background: #1a202c;
      color: white;
      outline: none;
    }

    .form-control::placeholder {
      color: #718096;
    }
    
    .btn-login {
      background: #F59E0B;
      border: none;
      border-radius: 8px;
      padding: 14px;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
      width: 100%;
      color: white;
      margin-top: 0.5rem;
    }
    
    .btn-login:hover {
      background: #D97706;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(245, 158, 11, 0.4);
      color: white;
    }
    
    .alert {
      border-radius: 8px;
      border: none;
      font-size: 14px;
      margin-bottom: 1rem;
    }
    
    .input-group {
      margin-bottom: 1.25rem;
    }
    
    @keyframes fadeInRight {
      0% {
        opacity: 0;
        transform: translateX(30px);
      }
      100% {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @media (max-width: 968px) {
      .login-wrapper {
        flex-direction: column;
      }

      .left-panel {
        min-height: 300px;
      }

      .left-content h1 {
        font-size: 1.8rem;
      }
    }
  </style>
</head>
<body>
  <div class="login-wrapper">
    <!-- Left Panel - Visual -->
    <div class="left-panel">
      <div class="left-content">
        <div class="logo">
          <i class="bi bi-speedometer2"></i>
        </div>
        <h1>Admin Control Center</h1>
        <p>Manage routes, schedules, and monitor your entire transport system efficiently</p>
      </div>
      <div class="carousel-indicators">
        <button class="active"></button>
        <button></button>
        <button></button>
      </div>
    </div>

    <!-- Right Panel - Form -->
    <div class="right-panel">
      <div class="login-container">
        <a href="/" class="back-link">
          <i class="bi bi-arrow-left"></i>
          Back to website
        </a>

        <div class="login-header">
          <span class="admin-badge">
            <i class="bi bi-shield-check"></i> Admin Access
          </span>
          <h2>Admin Login</h2>
          <p style="color: #a0aec0;">Secure access to management portal</p>
        </div>
        
        <% if (error) { %>
          <div class="alert alert-danger" role="alert">
            <i class="bi bi-exclamation-triangle"></i> <%= error %>
          </div>
        <% } %>
        
        <form action="/auth/admin/login" method="POST">
          <div class="input-group">
            <label for="email" class="form-label">Admin Email</label>
            <input 
              type="email" 
              class="form-control" 
              id="email"
              name="email" 
              placeholder="Enter admin email"
              required
              autocomplete="email"
            >
          </div>
          
          <div class="input-group">
            <label for="password" class="form-label">Password</label>
            <input 
              type="password" 
              class="form-control" 
              id="password"
              name="password" 
              placeholder="Enter admin password"
              required
              autocomplete="current-password"
            >
          </div>
          
          <button type="submit" class="btn btn-primary btn-login">
            <i class="bi bi-box-arrow-in-right"></i> Login as Admin
          </button>
        </form>

        <div style="text-align: center; margin-top: 1rem;">
          <a href="/auth/forgot-password" style="color: #F59E0B; text-decoration: none; font-size: 0.9rem;">
            <i class="bi bi-key"></i> Forgot password?
          </a>
        </div>

        <div style="text-align: center; margin-top: 2rem; padding-top: 1.5rem; border-top: 1px solid #4a5568;">
          <p style="color: #a0aec0; margin: 0;">Regular user? <a href="/auth/user/login" style="color: #F59E0B; text-decoration: none; font-weight: 500;">Login here</a></p>
        </div>

        <div style="text-align: center; margin-top: 1.5rem;">
          <small style="color: #718096;">
            <i class="bi bi-info-circle"></i> Default: admin@transport.com / admin123
          </small>
        </div>
      </div>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.querySelector('form');
      const submitBtn = document.querySelector('.btn-login');
      
      form.addEventListener('submit', function() {
        submitBtn.innerHTML = '<span class="spinner-border spinner-border-sm" role="status"></span> Authenticating...';
        submitBtn.disabled = true;
      });
      
      document.querySelector('input[name="email"]').focus();
    });
  </script>
</body>
</html>
