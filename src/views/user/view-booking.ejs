<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - Triply
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: #1E293B;
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #1E293B;
        }

        .dashboard-container {
            background: #FFFFFF;
            min-height: 100vh;
            border-radius: 20px 0 0 20px;
            box-shadow: -10px 0 30px rgba(0, 0, 0, 0.1);
            animation: slideIn 0.8s ease-out;
        }

        .user-header {
            background: #1E293B;
            color: white;
            padding: 2rem 0;
            border-radius: 20px 0 0 0;
        }

        .user-title {
            background: linear-gradient(135deg, #FFFFFF 0%, #F8FAFC 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 700;
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }

        .btn-custom {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 10px;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            color: white;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            position: relative;
            z-index: 1;
        }

        .btn-custom:hover {
            background: linear-gradient(135deg, #5a67d8 0%, #6b46a0 100%);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
            color: white;
            text-decoration: none;
        }

        .btn-outline-custom {
            background: transparent;
            border: 2px solid #667eea;
            color: #667eea;
            border-radius: 10px;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            position: relative;
            z-index: 1;
        }

        .btn-outline-custom:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.3);
            text-decoration: none;
        }

        .booking-details-card {
            border: 2px solid #764ba2;
            border-radius: 15px;
            background: linear-gradient(135deg, #F8FAFC 0%, #FFFFFF 100%);
            box-shadow: 0 4px 15px rgba(236, 72, 153, 0.1);
        }

        .detail-row {
            border-bottom: 1px solid #E2E8F0;
            padding: 15px 0;
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .booking-id {
            font-size: 1.1rem;
            font-weight: 500;
            color: #764ba2;
            text-align: center;
            margin-bottom: 1.2rem;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(-30px);
            }

            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .action-buttons .btn {
            min-width: 180px;
            padding: 12px 24px;
            font-weight: 600;
            border-radius: 25px;
        }

        .passenger-card {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 10px;
            border-left: 4px solid #007bff;
        }
    </style>
</head>

<body>

    <div class="dashboard-container">
        <div class="user-header">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col">
                        <a href="/user/<%= user.id %>/dashboard" class="text-decoration-none">
                            <h1 class="user-title">
                                <i class="bi bi-bus-front me-3"></i>
                                Triply
                            </h1>
                        </a>
                    </div>
                    <div class="col-auto">
                        <a href="/user/<%= user.id %>/dashboard" class="btn-outline-custom me-2">
                            <i class="fas fa-tachometer-alt"></i> Dashboard
                        </a>
                        <a href="/user/<%= user.id %>/bookings" class="btn-outline-custom me-2">
                            <i class="fas fa-list"></i> My Bookings
                        </a>
                        <a href="/auth/logout" class="btn-outline-custom">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="container mt-4">
            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6">

                    <!-- Booking ID Display -->
                    <div class="booking-id mt-4">
                        <div class="small text-muted" style="font-size:0.95rem; font-weight:400;">Your Booking ID</div>
                        <div style="font-size:1.2rem; font-weight:600; color:#4f378b; margin-top:0.2rem;">
                            <%= booking.bookingReference %>
                        </div>
                    </div>

                    <!-- Booking Details Card -->
                    <div class="card booking-details-card shadow-sm mb-4">
                        <div class="card-header text-center"
                            style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: #fff;">
                            <h5 class="mb-0">
                                <i class="bi bi-ticket-perforated me-2"></i>
                                Booking Details
                            </h5>
                        </div>
                        <div class="card-body">
                            <!-- Route Information -->
                            <div class="detail-row">
                                <div class="row">
                                    <div class="col-4">
                                        <strong class="text-muted">Route:</strong>
                                    </div>
                                    <div class="col-8">
                                        <span class="badge bg-primary me-2">
                                            <%= booking.schedule?.route?.routeNumber || 'N/A' %>
                                        </span>
                                        <%= booking.schedule?.route?.origin || 'N/A' %> → <%=
                                                booking.schedule?.route?.destination || 'N/A' %>
                                    </div>
                                </div>
                            </div>

                            <!-- Journey Details -->
                            <div class="detail-row">
                                <div class="row">
                                    <div class="col-4">
                                        <strong class="text-dark">Journey Date:</strong>
                                    </div>
                                    <div class="col-8">
                                        <i class="bi bi-calendar3 me-2"></i>
                                        <span class="text-dark">
                                            <% if (booking.schedule?.journeyDate) { %>
                                                <%= new Date(booking.schedule.journeyDate).toLocaleDateString('en-US', {
                                                    weekday: 'long' , year: 'numeric' , month: 'long' , day: 'numeric'
                                                    }) %>
                                                    <% } else { %>
                                                        N/A
                                                        <% } %>
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <!-- Time Details -->
                            <div class="detail-row">
                                <div class="row">
                                    <div class="col-4">
                                        <strong class="text-dark">Departure:</strong>
                                    </div>
                                    <div class="col-8">
                                        <i class="bi bi-clock me-2"></i>
                                        <span class="text-dark">
                                            <%= booking.schedule?.departureTime || 'N/A' %>
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <div class="detail-row">
                                <div class="row">
                                    <div class="col-4">
                                        <strong class="text-dark">Arrival:</strong>
                                    </div>
                                    <div class="col-8">
                                        <i class="bi bi-clock me-2"></i>
                                        <span class="text-dark">
                                            <%= booking.schedule?.arrivalTime || 'N/A' %>
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <!-- Bus Details -->
                            <% if (booking.schedule?.busNumber) { %>
                                <div class="detail-row">
                                    <div class="row">
                                        <div class="col-4">
                                            <strong class="text-dark">Bus Number:</strong>
                                        </div>
                                        <div class="col-8">
                                            <i class="bi bi-bus-front me-2"></i>
                                            <span class="text-dark">
                                                <%= booking.schedule.busNumber %>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <% } %>

                                    <% if (booking.schedule?.driverName) { %>
                                        <div class="detail-row">
                                            <div class="row">
                                                <div class="col-4">
                                                    <strong class="text-dark">Driver:</strong>
                                                </div>
                                                <div class="col-8">
                                                    <i class="bi bi-person-circle me-2"></i>
                                                    <span class="text-dark">
                                                        <%= booking.schedule.driverName %>
                                                    </span>
                                                </div>
                                            </div>
                                        </div>
                                        <% } %>

                                            <!-- Passenger Information -->
                                            <div class="detail-row">
                                                <div class="row">
                                                    <div class="col-4">
                                                        <strong class="text-dark">Passengers:</strong>
                                                    </div>
                                                    <div class="col-8">
                                                        <div class="badge bg-info mb-2">
                                                            <i class="bi bi-people me-1"></i>
                                                            <span class="text-dark">
                                                                <%= booking.seats || booking.passengers?.length || 1 %>
                                                                    Passenger(s)
                                                            </span>
                                                        </div>
                                                        <% if (booking.passengers && booking.passengers.length> 0) { %>
                                                            <% booking.passengers.forEach((passenger, index)=> { %>
                                                                <div class="passenger-card">
                                                                    <div class="row align-items-center">
                                                                        <div class="col-md-6">
                                                                            <strong class="text-dark">
                                                                                <%= passenger.name %>
                                                                            </strong>
                                                                        </div>
                                                                        <div class="col-md-3">
                                                                            <small class="text-muted">Age: <%=
                                                                                    passenger.age %></small>
                                                                        </div>
                                                                        <div class="col-md-3">
                                                                            <span class="badge bg-light text-dark">
                                                                                <%= passenger.gender %>
                                                                            </span>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                                <% }) %>
                                                                    <% } %>
                                                    </div>
                                                </div>
                                            </div>

                                            <!-- Seat Numbers -->
                                            <% if (booking.seatNumbers && booking.seatNumbers.length> 0) { %>
                                                <div class="detail-row">
                                                    <div class="row">
                                                        <div class="col-4">
                                                            <strong class="text-dark">Seats:</strong>
                                                        </div>
                                                        <div class="col-8">
                                                            <i class="bi bi-grid-3x3 me-2"></i>
                                                            <span class="text-dark">
                                                                <%= booking.seatNumbers.sort((a,b)=>a-b).join(', ') %>
                                                            </span>
                                                        </div>
                                                    </div>
                                                </div>
                                                <% } %>

                                                    <!-- Contact Information -->
                                                    <% if (booking.contactPhone) { %>
                                                        <div class="detail-row">
                                                            <div class="row">
                                                                <div class="col-4">
                                                                    <strong class="text-dark">Contact:</strong>
                                                                </div>
                                                                <div class="col-8">
                                                                    <i class="bi bi-telephone me-2"></i>
                                                                    <span class="text-dark">
                                                                        <%= booking.contactPhone %>
                                                                    </span>
                                                                </div>
                                                            </div>
                                                        </div>
                                                        <% } %>

                                                            <!-- Booking Status -->
                                                            <div class="detail-row">
                                                                <div class="row">
                                                                    <div class="col-4">
                                                                        <strong class="text-dark">Status:</strong>
                                                                    </div>
                                                                    <div class="col-8">
                                                                        <span
                                                                            class="badge bg-<%= booking.status === 'confirmed' ? 'success' : booking.status === 'cancelled' ? 'danger' : 'warning' %>">
                                                                            <%= booking.status?.charAt(0).toUpperCase()
                                                                                + booking.status?.slice(1) || 'Unknown'
                                                                                %>
                                                                        </span>
                                                                    </div>
                                                                </div>
                                                            </div>

                                                            <!-- Payment Information -->
                                                            <div class="detail-row pt-3">
                                                                <div class="row pt-3"
                                                                    style="border-top: 2px dashed #eee">
                                                                    <div class="col-4">
                                                                        <strong class="text-muted">Total Price:</strong>
                                                                    </div>
                                                                    <div class="col-8">
                                                                        <h5 class="text-success mb-0">
                                                                            <i class="bi bi-currency-rupee"></i>
                                                                            <%= booking.totalPrice?.toFixed(2) ||
                                                                                booking.price?.toFixed(2) || '0.00' %>
                                                                        </h5>
                                                                        <% if (booking.paymentMethod) { %>
                                                                            <small class="text-muted">
                                                                                Payment via <%=
                                                                                    booking.paymentMethod?.toUpperCase()
                                                                                    %>
                                                                                    <span class="badge bg-success ms-2">
                                                                                        <%= booking.paymentStatus?.toUpperCase()
                                                                                            || 'PAID' %>
                                                                                    </span>
                                                                            </small>
                                                                            <% } %>
                                                                    </div>
                                                                </div>
                                                            </div>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="d-grid gap-3 action-buttons pb-5">
                        <div class="row">
                            <div class="col-md-6 mb-2">
                                <a href="/user/<%= user.id %>/bookings" class="btn w-100"
                                    style="background: linear-gradient(135deg, #4f378b 0%, #312e81 100%); color: #fff; font-weight:600; border:none;">
                                    <i class="bi bi-list-ul me-2"></i>
                                    Back to Bookings
                                </a>
                            </div>
                            <div class="col-md-6 mb-2">
                                <button onclick="window.print()" class="btn btn-outline-secondary w-100">
                                    <i class="bi bi-printer me-2"></i>
                                    Print Details
                                </button>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

        <script>
            // Print function optimization
            window.addEventListener('beforeprint', function () {
                document.body.classList.add('printing');
            });

            window.addEventListener('afterprint', function () {
                document.body.classList.remove('printing');
            });

            // Add print styles
            const printStyles = `
            @media print {
                .btn, .alert-info, nav, .action-buttons { display: none !important; }
                .container { max-width: none !important; }
                .booking-details-card { border: 2px solid #000 !important; }
            }
        `;

            const styleSheet = document.createElement('style');
            styleSheet.textContent = printStyles;
            document.head.appendChild(styleSheet);
        </script>
    </div>
</body>

</html>