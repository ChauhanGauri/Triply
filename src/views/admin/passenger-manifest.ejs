<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || "Passenger Manifest" %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <style>
    :root {
      --primary-dark: #1E293B;
      --primary-blue: #3B82F6;
      --accent-amber: #F59E0B;
      --accent-pink: #EC4899;
      --text-light: #F8FAFC;
      --text-dark: #0F172A;
      --border-light: #E2E8F0;
      --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    body {
      background: linear-gradient(135deg, #F8FAFC 0%, #FFFFFF 100%);
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      min-height: 100vh;
      margin: 0;
    }

    /* Dashboard Header */
    .dashboard-header {
      background: var(--primary-dark);
      color: var(--text-light);
      padding: 1.5rem 0;
      box-shadow: var(--shadow);
    }

    .dashboard-title {
      color: var(--text-light);
      font-weight: 700;
      font-size: 1.75rem;
      margin: 0;
    }

    .dashboard-title .bi {
      color: var(--accent-amber);
      font-size: 2rem;
    }

    .btn-custom {
      background: linear-gradient(135deg, #F59E0B 0%, #D97706 100%);
      border: none;
      border-radius: 12px;
      padding: 0.75rem 1.5rem;
      font-weight: 600;
      color: white;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }

    .btn-custom:hover {
      background: linear-gradient(135deg, #D97706 0%, #B45309 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(245, 158, 11, 0.3);
      color: white;
      text-decoration: none;
    }

    .btn-outline-custom {
      background: transparent;
      border: 2px solid #F59E0B;
      color: #F59E0B;
      border-radius: 12px;
      padding: 0.75rem 1.5rem;
      font-weight: 600;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }

    .btn-outline-custom:hover {
      background: #F59E0B;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(245, 158, 11, 0.3);
      text-decoration: none;
    }

    /* Section Card */
    .section-card {
      background: white;
      border-radius: 12px;
      box-shadow: var(--shadow);
      padding: 2rem;
      margin-bottom: 2rem;
    }

    .section-title {
      color: var(--text-dark);
      font-weight: 600;
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .section-title .bi {
      color: var(--primary-blue);
    }

    /* Table Styling */
    .table {
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: var(--shadow);
    }

    .table thead th {
      background: var(--primary-dark);
      color: var(--text-light);
      border: none;
      font-weight: 600;
      padding: 1rem;
    }

    .table tbody td {
      padding: 1rem;
      border-bottom: 1px solid var(--border-light);
      vertical-align: middle;
    }

    .table tbody tr:hover {
      background: #F8FAFC;
    }

    /* Button Styling */
    .btn-primary {
      background: var(--primary-blue);
      border: none;
      padding: 0.5rem 1.5rem;
      border-radius: 6px;
      font-weight: 500;
      transition: all 0.2s ease;
    }

    .btn-primary:hover {
      background: #2563EB;
      transform: translateY(-1px);
    }

    .btn-success {
      background: #10B981;
      border: none;
      padding: 0.5rem 1.5rem;
      border-radius: 6px;
      font-weight: 500;
      transition: all 0.2s ease;
    }

    .btn-success:hover {
      background: #059669;
      transform: translateY(-1px);
    }

    .btn-danger {
      background: #EF4444;
      border: none;
      padding: 0.5rem 1.5rem;
      border-radius: 6px;
      font-weight: 500;
      transition: all 0.2s ease;
    }

    .btn-danger:hover {
      background: #DC2626;
      transform: translateY(-1px);
    }

    /* Alert Styling */
    .alert {
      border: none;
      border-radius: 8px;
      padding: 1rem 1.5rem;
      margin-bottom: 1.5rem;
    }

    .alert-success {
      background: #D1FAE5;
      color: #065F46;
    }

    .alert-danger {
      background: #FEE2E2;
      color: #991B1B;
    }

    /* Manifest Header */
    .manifest-header {
      background-color: #1E293B; 
      color: white;
      border-radius: 8px;
      padding: 2rem;
      margin-bottom: 2rem;
    }

    .passenger-row {
      transition: background-color 0.2s ease;
    }

    .passenger-row:hover {
      background-color: #f8f9fa;
    }

    .boarding-status {
      min-width: 120px;
    }

    .checkin-btn {
      min-width: 80px;
    }

    @media print {
      .no-print { display: none !important; }
      .manifest-header { background: var(--primary-blue) !important; color: white !important; }
      body { font-size: 12px; }
      .table { font-size: 11px; }
    }

    .status-not-boarded { background-color: #FEF3C7; color: #92400E; }
    .status-boarded { background-color: #DBEAFE; color: #1E40AF; }
    .status-no-show { background-color: #FEE2E2; color: #991B1B; }
  </style>
</head>
<body class="bg-light">

  <!-- Header -->
  <div class="dashboard-header">
    <div class="container">
      <div class="row align-items-center">
        <div class="col">
          <a href="/admin/dashboard" class="text-decoration-none">
            <h1 class="dashboard-title mb-1">
              <i class="bi bi-bus-front me-3"></i>
              Triply
            </h1>
          </a>
          <p class="mb-0 opacity-75">Admin Dashboard</p>
        </div>
        <div class="col-auto">
          <div class="d-flex gap-2 flex-wrap">
            <a href="/admin/bookings" class="btn-custom">
              <i class="bi bi-ticket-detailed"></i>
              Bookings
            </a>
            <a href="/admin/routes" class="btn-custom">
              <i class="bi bi-map"></i>
              Routes
            </a>
            <a href="/admin/schedules" class="btn-custom">
              <i class="bi bi-calendar-event"></i>
              Schedules
            </a>
            <a href="/admin/users" class="btn-custom">
              <i class="bi bi-people"></i>
              Users
            </a>
            <a href="/auth/logout" class="btn-outline-custom">
              <i class="bi bi-box-arrow-right"></i>
              Logout
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="container mt-4">
    <div class="section-card">
      <h2 class="section-title">
        <i class="bi bi-clipboard-check"></i>
        Passenger Manifest
      </h2>

      <!-- Success/Error Messages -->
      <% if (typeof success !== 'undefined' && success) { %>
        <div class="alert alert-success alert-dismissible fade show no-print" role="alert">
          <i class="bi bi-check-circle-fill me-2"></i>
          <%= success %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>
      
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger alert-dismissible fade show no-print" role="alert">
          <i class="bi bi-exclamation-triangle-fill me-2"></i>
          <%= error %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>

      <!-- Manifest Header -->
      <div class="manifest-header">
      <div class="row align-items-center">
        <div class="col-md-8">
          <h1 class="mb-1">
            <i class="bi bi-people-fill me-2"></i>
            Passenger Manifest
          </h1>
          <h2 class="h4 mb-2">
            Route <%= manifest.schedule?.route?.routeNumber || 'N/A' %>: 
            <%= manifest.schedule?.route?.origin || 'N/A' %> → 
            <%= manifest.schedule?.route?.destination || 'N/A' %>
          </h2>
          <div class="row">
            <div class="col-sm-6">
              <p class="mb-1">
                <i class="bi bi-calendar3 me-2"></i>
                <strong>Date:</strong> 
                <% if (manifest.schedule?.journeyDate) { %>
                  <%= new Date(manifest.schedule.journeyDate).toLocaleDateString('en-IN', { 
                    weekday: 'long', 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric' 
                  }) %>
                <% } else { %>
                  N/A
                <% } %>
              </p>
              <p class="mb-1">
                <i class="bi bi-clock me-2"></i>
                <strong>Departure:</strong> <%= manifest.schedule?.departureTime || 'N/A' %>
              </p>
              <p class="mb-0">
                <i class="bi bi-clock-fill me-2"></i>
                <strong>Arrival:</strong> <%= manifest.schedule?.arrivalTime || 'N/A' %>
              </p>
            </div>
            <div class="col-sm-6">
              <% if (manifest.schedule?.busNumber) { %>
                <p class="mb-1">
                  <i class="bi bi-truck me-2"></i>
                  <strong>Bus Number:</strong> <%= manifest.schedule.busNumber %>
                </p>
              <% } %>
              <% if (manifest.schedule?.driverName) { %>
                <p class="mb-1">
                  <i class="bi bi-person-badge me-2"></i>
                  <strong>Driver:</strong> <%= manifest.schedule.driverName %>
                </p>
              <% } %>
              <p class="mb-0">
                <i class="bi bi-shield-check me-2"></i>
                <strong>Status:</strong> 
                <span class="badge bg-light text-dark">
                  <%= manifest.manifestStatus.charAt(0).toUpperCase() + manifest.manifestStatus.slice(1) %>
                </span>
              </p>
            </div>
          </div>
        </div>
        <div class="col-md-4 text-md-end">
          <div class="row text-center">
            <div class="col-6">
              <div class="h2 mb-0"><%= manifest.totalPassengers || 0 %></div>
              <small>Total Passengers</small>
            </div>
            <div class="col-6">
              <div class="h2 mb-0"><%= manifest.totalSeatsBooked || 0 %></div>
              <small>Seats Booked</small>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Action Buttons -->
    <div class="d-flex justify-content-between align-items-center mb-4 no-print">
      <div>
        <a href="/admin/manifests" class="btn-outline-custom">
          <i class="bi bi-arrow-left"></i> Back to Manifests
        </a>
        <a href="/admin/schedules/<%= manifest.schedule?._id || '' %>" class="btn-outline-custom">
          <i class="bi bi-calendar3"></i> View Schedule
        </a>
      </div>
      <div class="d-flex gap-2">
        <button class="btn-outline-custom" onclick="window.print()">
          <i class="bi bi-printer"></i> Print Manifest
        </button>
        <% if (manifest.manifestStatus === 'draft') { %>
          <button class="btn-success" onclick="finalizeManifest()">
            <i class="bi bi-check-circle"></i> Finalize Manifest
          </button>
        <% } %>
        <% if (manifest.manifestStatus === 'finalized') { %>
          <button class="btn-primary" onclick="markDeparted()">
            <i class="bi bi-airplane-engines"></i> Mark as Departed
          </button>
        <% } %>
      </div>
    </div>

    <!-- Passenger List -->
    <div class="section-card">
      <h3 class="section-title">
        <i class="bi bi-list-ul"></i>
        Passenger List
        <span class="badge" style="background: var(--primary-blue); color: var(--text-light); margin-left: 0.5rem;"><%= manifest.passengers?.length || 0 %> passengers</span>
      </h3>
      <% if (manifest.passengers && manifest.passengers.length > 0) { %>
        <div class="table-responsive">
          <table class="table mb-0">
            <thead>
              <tr>
                <th width="5%">#</th>
                <th width="20%">Passenger Name</th>
                <th width="10%">Age</th>
                <th width="10%">Gender</th>
                <th width="15%">Booking Ref</th>
                <th width="15%">Contact</th>
                <th width="15%" class="no-print">Boarding Status</th>
                  <th width="10%" class="no-print">Check-in</th>
                </tr>
              </thead>
              <tbody>
                <% manifest.passengers.forEach((passenger, index) => { %>
                  <tr class="passenger-row">
                    <td class="align-middle">
                      <span class="badge bg-secondary"><%= index + 1 %></span>
                    </td>
                    <td class="align-middle">
                      <div class="fw-bold"><%= passenger.passengerDetails?.name || 'N/A' %></div>
                      <small class="text-muted">
                        <%= passenger.user?.name || 'User N/A' %>
                      </small>
                    </td>
                    <td class="align-middle">
                      <%= passenger.passengerDetails?.age || 'N/A' %>
                    </td>
                    <td class="align-middle">
                      <% if (passenger.passengerDetails?.gender) { %>
                        <span class="badge bg-<%= 
                          passenger.passengerDetails.gender === 'Male' ? 'primary' : 
                          passenger.passengerDetails.gender === 'Female' ? 'info' : 'secondary' 
                        %>">
                          <%= passenger.passengerDetails.gender %>
                        </span>
                      <% } else { %>
                        N/A
                      <% } %>
                    </td>
                    <td class="align-middle">
                      <% if (passenger.bookingReference) { %>
                        <code class="small"><%= passenger.bookingReference %></code>
                      <% } else { %>
                        <span class="text-muted">Legacy</span>
                      <% } %>
                    </td>
                    <td class="align-middle">
                      <% if (passenger.contactPhone) { %>
                        <a href="tel:<%= passenger.contactPhone %>" class="text-decoration-none">
                          <i class="bi bi-telephone"></i> <%= passenger.contactPhone %>
                        </a>
                      <% } else { %>
                        <span class="text-muted">No phone</span>
                      <% } %>
                    </td>
                    <td class="align-middle no-print">
                      <select class="form-select form-select-sm boarding-status status-<%= passenger.passengerDetails?.boardingStatus || 'not-boarded' %>" 
                              onchange="updateBoardingStatus('<%= passenger._id %>', this.value)">
                        <option value="not-boarded" <%= (passenger.passengerDetails?.boardingStatus || 'not-boarded') === 'not-boarded' ? 'selected' : '' %>>
                          Not Boarded
                        </option>
                        <option value="boarded" <%= passenger.passengerDetails?.boardingStatus === 'boarded' ? 'selected' : '' %>>
                          Boarded
                        </option>
                        <option value="no-show" <%= passenger.passengerDetails?.boardingStatus === 'no-show' ? 'selected' : '' %>>
                          No Show
                        </option>
                      </select>
                    </td>
                    <td class="align-middle no-print">
                      <button class="btn btn-sm checkin-btn <%= passenger.checkedIn ? 'btn-success' : 'btn-outline-primary' %>" 
                              onclick="toggleCheckIn('<%= passenger._id %>', <%= !passenger.checkedIn %>)">
                        <% if (passenger.checkedIn) { %>
                          <i class="bi bi-check-circle-fill"></i>
                        <% } else { %>
                          <i class="bi bi-circle"></i>
                        <% } %>
                      </button>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } else { %>
          <div class="text-center py-5">
            <i class="bi bi-people fs-1 text-muted"></i>
            <h5 class="mt-3">No Passengers Yet</h5>
            <p class="text-muted">No confirmed bookings found for this schedule.</p>
          </div>
        <% } %>
      </div>
    </div>

    <!-- Summary Footer -->
    <div class="row mt-4">
      <div class="col-md-6">
        <div class="section-card">
          <h4 class="section-title" style="font-size: 1.25rem; margin-bottom: 1.5rem;">
            <i class="bi bi-bar-chart"></i>
            Boarding Summary
          </h4>
          <div class="row text-center">
            <div class="col-4">
              <div class="h5 mb-0" style="color: var(--accent-amber);">
                <%= manifest.passengers?.filter(p => p.passengerDetails?.boardingStatus === 'not-boarded').length || 0 %>
              </div>
              <small style="color: var(--text-dark);">Not Boarded</small>
            </div>
            <div class="col-4">
              <div class="h5 mb-0" style="color: #10B981;">
                <%= manifest.passengers?.filter(p => p.passengerDetails?.boardingStatus === 'boarded').length || 0 %>
              </div>
              <small style="color: var(--text-dark);">Boarded</small>
            </div>
            <div class="col-4">
              <div class="h5 mb-0" style="color: #EF4444;">
                <%= manifest.passengers?.filter(p => p.passengerDetails?.boardingStatus === 'no-show').length || 0 %>
              </div>
              <small style="color: var(--text-dark);">No Show</small>
            </div>
          </div>
        </div>
      </div>
      <div class="col-md-6">
        <div class="section-card">
          <h4 class="section-title" style="font-size: 1.25rem; margin-bottom: 1.5rem;">
            <i class="bi bi-check-circle"></i>
            Check-in Summary
          </h4>
          <div class="row text-center">
            <div class="col-6">
              <div class="h5 mb-0" style="color: #10B981;">
                <%= manifest.passengers?.filter(p => p.checkedIn).length || 0 %>
              </div>
              <small style="color: var(--text-dark);">Checked In</small>
            </div>
            <div class="col-6">
              <div class="h5 mb-0" style="color: var(--accent-amber);">
                <%= manifest.passengers?.filter(p => !p.checkedIn).length || 0 %>
              </div>
              <small style="color: var(--text-dark);">Pending</small>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    function updateBoardingStatus(passengerId, status) {
      fetch(`/admin/manifests/<%= manifest._id %>/passengers/${passengerId}/status`, {
        method: 'PATCH',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ boardingStatus: status })
      })
      .then(response => response.json())
      .then(data => {
        if (data.message) {
          // Update the select element's class based on new status
          const selectElement = event.target;
          selectElement.className = `form-select form-select-sm boarding-status status-${status}`;
          
          // Show success message briefly
          showToast(`Passenger status updated to ${status}`, 'success');
        }
      })
      .catch(error => {
        console.error('Error:', error);
        showToast('Error updating passenger status', 'error');
      });
    }

    function toggleCheckIn(passengerId, checkedIn) {
      fetch(`/admin/manifests/<%= manifest._id %>/passengers/${passengerId}/status`, {
        method: 'PATCH',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ checkedIn: checkedIn })
      })
      .then(response => response.json())
      .then(data => {
        if (data.message) {
          location.reload(); // Reload to update UI
        }
      })
      .catch(error => {
        console.error('Error:', error);
        showToast('Error updating check-in status', 'error');
      });
    }

    function finalizeManifest() {
      if (confirm('Are you sure you want to finalize this manifest? This action cannot be undone.')) {
        fetch(`/admin/manifests/<%= manifest._id %>/finalize`, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          }
        })
        .then(response => {
          if (response.ok) {
            location.reload();
          } else {
            return response.json().then(data => {
              throw new Error(data.message || 'Error finalizing manifest');
            });
          }
        })
        .catch(error => {
          console.error('Error:', error);
          alert('Error finalizing manifest: ' + error.message);
        });
      }
    }

    function markDeparted() {
      if (confirm('Mark this bus as departed?')) {
        fetch(`/admin/manifests/<%= manifest._id %>/departed`, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          }
        })
        .then(response => {
          if (response.ok) {
            location.reload();
          } else {
            return response.json().then(data => {
              throw new Error(data.message || 'Error marking as departed');
            });
          }
        })
        .catch(error => {
          console.error('Error:', error);
          alert('Error marking as departed: ' + error.message);
        });
      }
    }

    function showToast(message, type) {
      // Simple toast notification
      const toast = document.createElement('div');
      toast.className = `alert alert-${type === 'success' ? 'success' : 'danger'} alert-dismissible fade show position-fixed`;
      toast.style.top = '20px';
      toast.style.right = '20px';
      toast.style.zIndex = '9999';
      toast.innerHTML = `
        ${message}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      `;
      document.body.appendChild(toast);
      
      setTimeout(() => {
        toast.remove();
      }, 3000);
    }
  </script>
</body>
</html>