<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="<%= user && user.role === 'admin' ? '/admin/dashboard' : '/' %>">
      <i class="bi bi-bus-front"></i> Transport System
    </a>
    <% if (user && user.role === 'admin') { %>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/admin/dashboard">Dashboard</a>
        <a class="nav-link" href="/admin/routes">Routes</a>
        <a class="nav-link" href="/admin/schedules">Schedules</a>
        <a class="nav-link" href="/admin/manifests">Manifests</a>
        <a class="nav-link" href="/admin/users">Users</a>
        <a class="nav-link" href="/admin/bookings">Bookings</a>
        <a class="nav-link" href="/auth/logout">Logout</a>
      </div>
    <% } else if (user && user.role === 'user') { %>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/user/<%= user.id %>/dashboard">Dashboard</a>
        <a class="nav-link" href="/user/<%= user.id %>/bookings">My Bookings</a>
        <a class="nav-link" href="/auth/logout">Logout</a>
      </div>
    <% } else { %>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/auth/admin/login">Admin Login</a>
        <a class="nav-link" href="/auth/user/login">User Login</a>
      </div>
    <% } %>
  </div>
</nav>